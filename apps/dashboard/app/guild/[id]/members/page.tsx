"use client"; import * as React from "react"; import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card"; export default function Page({params}:{params:{id:string}}){ const [members,setMembers]=React.useState<any[]>([]); React.useEffect(()=>{ fetch('/api/guilds/'+params.id+'/members').then(r=>r.json()).then(j=>setMembers(j.members||j||[])); },[]); return <div className="space-y-4"><h1 className="text-xl font-bold">Members</h1><Card><CardHeader><CardTitle>Member List</CardTitle></CardHeader><CardContent>{members.length===0?<p className='text-sm text-muted-foreground'>No members or bot not in guild</p>:members.slice(0,20).map((m)=> <div key={m.user.id} className='flex justify-between border p-2 rounded mb-1'><span>{m.user.username} ({m.user.id})</span><span className='text-xs'>{m.roles?.length} roles</span></div>)}</CardContent></Card></div>; }

"use client"; import * as React from "react"; import {Card,CardHeader,CardTitle,CardContent} from "@/components/ui/card"; export default function Page({params}:{params:{id:string}}){ const [list,setList]=React.useState<any[]>([]); React.useEffect(()=>{ fetch('/api/guilds/'+params.id+'/suggestions').then(r=>r.json()).then(j=>setList(j.suggestions||j||[])); },[]); return <div className="space-y-4"><h1 className="text-xl font-bold">Suggestions</h1><Card><CardHeader><CardTitle>Suggestions</CardTitle></CardHeader><CardContent>{list.length===0?<p className='text-sm text-muted-foreground'>No suggestions</p>:list.map((s)=> <div key={s.id} className='border p-2 rounded mb-1'>{s.content} - {s.status}</div>)}</CardContent></Card></div>; }

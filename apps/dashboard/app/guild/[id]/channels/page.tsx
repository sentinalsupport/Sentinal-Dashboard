"use client"; import * as React from "react"; import {Card,CardHeader,CardTitle,CardContent} from "@/components/ui/card"; export default function Page({params}:{params:{id:string}}){ const [ch,setCh]=React.useState<any[]>([]); React.useEffect(()=>{ fetch('/api/guilds/'+params.id+'/channels').then(r=>r.json()).then(j=>setCh(j.channels||j||[])); },[]); return <div className="space-y-4"><h1 className="text-xl font-bold">Channels</h1><Card><CardHeader><CardTitle>Channels</CardTitle></CardHeader><CardContent>{ch.length===0?<p>None</p>:ch.map((c)=> <div key={c.id} className='border p-2 rounded mb-1'>{c.name} ({c.type}) - {c.id}</div>)}</CardContent></Card></div>; }
